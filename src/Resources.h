#pragma once

struct StaticImages {
	enum Id {
		PlayerHP,
		EnemyHP,
		Mushroom,
		Shield,
		Speed,
		IconShield,
		IconSpeed,
		AuraShield,
		LevelMap,
		End
	};
};

struct RotatedImages {
	enum Id {
		Human,
		Enemy,
		Bullet,
		End
	};
};
