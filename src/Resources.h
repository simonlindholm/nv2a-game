#pragma once

struct StaticImages {
	enum Id {
		Bullet,
		PlayerHP,
		EnemyHP,
		Mushroom,
		Shield,
		Speed,
		LevelMap,
		End
	};
};

struct RotatedImages {
	enum Id {
		Human,
		Enemy,
		End
	};
};
